# Local dev uses the embedded Postgres started by `npm run db` (scripts/db.ts).
# In production point this at any real Postgres — the schema is standard.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/siddhatva?schema=public"
PORT=4000
CORS_ORIGIN=http://localhost:3000
